<?xml version="1.0" encoding="UTF-8"?>
<RiskProfileConf xmlns="http://example.com/kpmr"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="http://example.com/kpmr ../xsd/KPMRRiskProfileConf.xsd">
   <aggregationConfig>
      <risikoName>Risiko Kredit</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Kredit']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Kredit']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;

              print(threshold);
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Operasional</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Operasional']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Operasional']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Hukum</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Hukum']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Hukum']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Intragrup</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Intragrup']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Intragrup']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Asuransi</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Asuransi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Asuransi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Investasi</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Investasi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Investasi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Imbal Hasil</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Imbal Hasil']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Imbal Hasil']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40,
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Likuiditas</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Likuiditas']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Likuiditas']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Pasar</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Pasar']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Pasar']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Reputasi</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Reputasi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Reputasi']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40,
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Stratejik</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Stratejik']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Stratejik']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
   <aggregationConfig>
      <risikoName>Risiko Kepatuhan</risikoName>
      <score_rule>
          <![CDATA[
              var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Kepatuhan']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }

              
             return total * 1.0;
          ]]>
      </score_rule>
      <rating_rule>
            <![CDATA[
            var nodes = retrieveNodesByXpath("//Node[risiko_name='Risiko Kepatuhan']");
              var total = 0.0;

              var converted = kpmrNodeTypeAsChaiVar(nodes);

              for (c : converted) {
                total += c["Computed_Score"] * 1.0;
              }
              
              var x = total;
              
              var r = ratingByThreshold(threshold, x);
              
              return r;
            
            ]]>
       </rating_rule>
       <rating_to_score>
             <![CDATA[
             5 : x<=40
4:  40 < x <=55
3:  55 < x <=70
2:  70 < x <=85
1:  85 < x <=100
             ]]>
       </rating_to_score>
   </aggregationConfig>
</RiskProfileConf>
